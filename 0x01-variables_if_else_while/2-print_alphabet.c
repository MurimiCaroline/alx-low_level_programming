#include <stdio.h>
#include <stdlib.h>
/**
 * main- entry point of the program.
 *
 * Return:gives 0 on success.
 */
int main(void)
	{
		char l;

	for (l = 'a'; l < = 'z'; l++)
		putchar (l);

		putchar('\n');
		return (0);
	}
