#include<stdio.h>
/*
 *main-Serves as the entry point of this program
 *Return: Returns zero by default if no errors otherwise returns a non zero value
 */
 int main (void)
{
	puts("\"Programming is like building a multilingual puzzle.");
	return (0);
}
